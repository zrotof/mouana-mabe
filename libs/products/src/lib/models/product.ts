export class Product{

    id!: string ;
    name!: string | Blob;
    price!: number | Blob;
    countInStock!: number | Blob;
    imageVitrine!: string | Blob;
    images!: string | Blob | any[];
    category!: string | Blob;
    forAdult!: boolean | Blob;
    color!: string | Blob;
    sexe!: string | Blob;
    detailsProduct!: string | Blob;
    topProduct!: boolean | Blob;
    sizeXS!: number | Blob;
    sizeS!: number | Blob;
    sizeM!: number | Blob;
    sizeL!: number | Blob;
    sizeXL!: number | Blob;
    sizeXXL!: number | Blob;
}